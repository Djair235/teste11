const handlebarsConfig = require("../../src/dependencies/handlebarsConfig")
const bodyparserConfig = require("../dependencies/bodyparserConfig")
const corsConfig = require("../dependencies/corsConfig")

module.exports = {
    handlebarsConfig,
    bodyparserConfig,
    corsConfig
}
